// Generated by CoffeeScript 1.6.3
module.exports = require('moment');

module.exports.fn.daySeconds = function(ds) {
  var s;
  if (ds != null) {
    s = Math.floor(ds);
    this.milliseconds((ds - s) * 1000);
    this.seconds(s % 60);
    this.minutes((s % 3600) / 60);
    return this.hours(s / 3600);
  } else {
    return this.milliseconds() / 1000 + this.seconds() + this.minutes() * 60 + this.hours() * 3600;
  }
};

module.exports.fn.dayTime = function(ds) {
  var p;
  if (ds != null) {
    p = ds.split(':');
    this.hours(parseFloat(p[0]));
    this.minutes(parseFloat(p[1]));
    this.seconds(0);
    return this.milliseconds(0);
  } else {
    return this.format('HH:mm');
  }
};

/*
//@ sourceMappingURL=moment-extended.map
*/
